// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"errors"
	"fmt"

	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/nexentra/aesir/ent/eval"
)

// EvalCreate is the builder for creating a Eval entity.
type EvalCreate struct {
	config
	mutation *EvalMutation
	hooks    []Hook
}

// SetSnippet sets the "snippet" field.
func (ec *EvalCreate) SetSnippet(s string) *EvalCreate {
	ec.mutation.SetSnippet(s)
	return ec
}

// SetResult sets the "result" field.
func (ec *EvalCreate) SetResult(s []string) *EvalCreate {
	ec.mutation.SetResult(s)
	return ec
}

// SetTime sets the "time" field.
func (ec *EvalCreate) SetTime(s string) *EvalCreate {
	ec.mutation.SetTime(s)
	return ec
}

// Mutation returns the EvalMutation object of the builder.
func (ec *EvalCreate) Mutation() *EvalMutation {
	return ec.mutation
}

// Save creates the Eval in the database.
func (ec *EvalCreate) Save(ctx context.Context) (*Eval, error) {
	return withHooks(ctx, ec.sqlSave, ec.mutation, ec.hooks)
}

// SaveX calls Save and panics if Save returns an error.
func (ec *EvalCreate) SaveX(ctx context.Context) *Eval {
	v, err := ec.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (ec *EvalCreate) Exec(ctx context.Context) error {
	_, err := ec.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (ec *EvalCreate) ExecX(ctx context.Context) {
	if err := ec.Exec(ctx); err != nil {
		panic(err)
	}
}

// check runs all checks and user-defined validators on the builder.
func (ec *EvalCreate) check() error {
	if _, ok := ec.mutation.Snippet(); !ok {
		return &ValidationError{Name: "snippet", err: errors.New(`ent: missing required field "Eval.snippet"`)}
	}
	if _, ok := ec.mutation.Result(); !ok {
		return &ValidationError{Name: "result", err: errors.New(`ent: missing required field "Eval.result"`)}
	}
	if _, ok := ec.mutation.Time(); !ok {
		return &ValidationError{Name: "time", err: errors.New(`ent: missing required field "Eval.time"`)}
	}
	return nil
}

func (ec *EvalCreate) sqlSave(ctx context.Context) (*Eval, error) {
	if err := ec.check(); err != nil {
		return nil, err
	}
	_node, _spec := ec.createSpec()
	if err := sqlgraph.CreateNode(ctx, ec.driver, _spec); err != nil {
		if sqlgraph.IsConstraintError(err) {
			err = &ConstraintError{msg: err.Error(), wrap: err}
		}
		return nil, err
	}
	id := _spec.ID.Value.(int64)
	_node.ID = int(id)
	ec.mutation.id = &_node.ID
	ec.mutation.done = true
	return _node, nil
}

func (ec *EvalCreate) createSpec() (*Eval, *sqlgraph.CreateSpec) {
	var (
		_node = &Eval{config: ec.config}
		_spec = sqlgraph.NewCreateSpec(eval.Table, sqlgraph.NewFieldSpec(eval.FieldID, field.TypeInt))
	)
	if value, ok := ec.mutation.Snippet(); ok {
		_spec.SetField(eval.FieldSnippet, field.TypeString, value)
		_node.Snippet = value
	}
	if value, ok := ec.mutation.Result(); ok {
		_spec.SetField(eval.FieldResult, field.TypeJSON, value)
		_node.Result = value
	}
	if value, ok := ec.mutation.Time(); ok {
		_spec.SetField(eval.FieldTime, field.TypeString, value)
		_node.Time = value
	}
	return _node, _spec
}

// EvalCreateBulk is the builder for creating many Eval entities in bulk.
type EvalCreateBulk struct {
	config
	err      error
	builders []*EvalCreate
}

// Save creates the Eval entities in the database.
func (ecb *EvalCreateBulk) Save(ctx context.Context) ([]*Eval, error) {
	if ecb.err != nil {
		return nil, ecb.err
	}
	specs := make([]*sqlgraph.CreateSpec, len(ecb.builders))
	nodes := make([]*Eval, len(ecb.builders))
	mutators := make([]Mutator, len(ecb.builders))
	for i := range ecb.builders {
		func(i int, root context.Context) {
			builder := ecb.builders[i]
			var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
				mutation, ok := m.(*EvalMutation)
				if !ok {
					return nil, fmt.Errorf("unexpected mutation type %T", m)
				}
				if err := builder.check(); err != nil {
					return nil, err
				}
				builder.mutation = mutation
				var err error
				nodes[i], specs[i] = builder.createSpec()
				if i < len(mutators)-1 {
					_, err = mutators[i+1].Mutate(root, ecb.builders[i+1].mutation)
				} else {
					spec := &sqlgraph.BatchCreateSpec{Nodes: specs}
					// Invoke the actual operation on the latest mutation in the chain.
					if err = sqlgraph.BatchCreate(ctx, ecb.driver, spec); err != nil {
						if sqlgraph.IsConstraintError(err) {
							err = &ConstraintError{msg: err.Error(), wrap: err}
						}
					}
				}
				if err != nil {
					return nil, err
				}
				mutation.id = &nodes[i].ID
				if specs[i].ID.Value != nil {
					id := specs[i].ID.Value.(int64)
					nodes[i].ID = int(id)
				}
				mutation.done = true
				return nodes[i], nil
			})
			for i := len(builder.hooks) - 1; i >= 0; i-- {
				mut = builder.hooks[i](mut)
			}
			mutators[i] = mut
		}(i, ctx)
	}
	if len(mutators) > 0 {
		if _, err := mutators[0].Mutate(ctx, ecb.builders[0].mutation); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

// SaveX is like Save, but panics if an error occurs.
func (ecb *EvalCreateBulk) SaveX(ctx context.Context) []*Eval {
	v, err := ecb.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (ecb *EvalCreateBulk) Exec(ctx context.Context) error {
	_, err := ecb.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (ecb *EvalCreateBulk) ExecX(ctx context.Context) {
	if err := ecb.Exec(ctx); err != nil {
		panic(err)
	}
}
